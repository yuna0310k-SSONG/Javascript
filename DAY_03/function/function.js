function makeCircle(x) {
  return `${x}색 동그라미`;
}

// 숫자 넣으면 세배로 돌려주는 함수
function three(y) {
  return y * 3;
}

//문자를 넣으면 맨 뒤에 느낌표 !!! 붙혀서 돌려주는 함수
function makeExclamation(x) {
  return `${x}!!!`;
}

const a = makeCircle("핑크");
console.log(a);

//input [매개변수/파라메터]
function def(x, y) {
  return x * Y;
}

const b = def(1, 5);
